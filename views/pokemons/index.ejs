<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Pokemons List</h1>
    <h2>Index Page</h2>
    <a href="/pokemons/new">Catch a new Pokemon</a>
    <ul>
      <% pokemons.forEach(pokemon => { %>
      <li>
        <%= pokemon._id %>"><%= pokemon.text %>
        - 
        <%= pokemons.caught ? "Yes" : "No"%>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
